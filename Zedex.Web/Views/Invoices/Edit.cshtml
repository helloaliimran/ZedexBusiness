@model InvoiceFormViewModel
@{
    ViewData["Title"] = $"Edit Invoice {ViewBag.InvoiceNumber}";
}

<h3 class="mb-3"><i class="bi bi-receipt me-2"></i>Edit Invoice @ViewBag.InvoiceNumber <span class="badge text-bg-secondary">Draft</span></h3>
<p class="text-muted">You can change items while the invoice is a draft. Post it to update stock and the customer ledger.</p>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />
    <partial name="_InvoiceForm" model="Model" />
    <button type="submit" class="btn btn-primary">
        <i class="bi bi-check-lg me-1"></i>Save changes
    </button>
    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        window.invoiceProducts = @Html.Raw(Json.Serialize(ViewBag.ProductsJson));
        window.invoiceCustomers = @Html.Raw(Json.Serialize(ViewBag.CustomersJson));
        window.invoiceInitialRows = @Html.Raw(Json.Serialize(Model.Items));
    </script>
    <script src="~/js/invoice-entry.js" asp-append-version="true"></script>
}
