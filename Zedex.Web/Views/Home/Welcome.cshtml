@{
    ViewData["Title"] = "Welcome";
}

<div class="text-center py-5">
    <i class="bi bi-box-seam-fill fs-1 text-primary"></i>
    <h3 class="mt-3">Welcome, @User.Identity!.Name</h3>
    <p class="text-muted">Use the menu on the left to open the modules you have access to.</p>
</div>
